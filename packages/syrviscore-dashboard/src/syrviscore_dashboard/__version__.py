"""SyrvisCore Dashboard version information."""

# Kept in lockstep with the SyrvisCore service version (syrviscore.__version__):
# the dashboard image tag == the service version, so the dashboard the CLI ships
# always matches the service it adapts. Synced by the release script.
__version__ = "0.5.2"
